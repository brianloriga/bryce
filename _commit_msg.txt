feat: lesson intro audio + glassmorphism intro screen

- GPT-4o generates lesson_intro (3-5 sentence spoken overview) and
  image_search_query (Pexels keywords) on every scan
- generate-questions fetches 3 Pexels stock photos and returns them as
  intro_image_urls so the parent can preview/remove before saving
- generate-audio extended: TTS for lesson_intro -> intro.mp3 uploaded to
  Storage; patches custom_units with intro_audio_url + intro_image_urls
- lesson_intro column saved immediately on unit creation so intro screen
  shows on first quiz open without needing to close and reopen
- ScanScreen: Lesson Intro Audio toggle + removable photo preview strip
- QuizScreen: glassmorphism intro screen using expo-blur (BlurView panels
  with white border over full-bleed Ken Burns Pexels photos), animated
  wave bars, left-to-right timing progress bar, ActivityIndicator while
  audio generates, background polling for intro_audio_url
- CHANGELOG updated
